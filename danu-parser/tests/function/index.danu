fn foo() { ... }
fn foo(a) { ... }
fn foo(a: A) { ... }

fn foo() -> Bar { ... }
fn foo(a) -> Bar { ... }
fn foo(a: A) -> Bar { ... }

fn foo() -> Bar = ...;
fn foo(a) -> Bar = ...;
fn foo(a: A) -> Bar = ...;
