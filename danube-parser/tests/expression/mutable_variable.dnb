fn foo() {
  let mut _ = false;
  let mut foo = false;
  let mut Foo::Bar = false;
  let mut Foo(foo) = false;
  let mut Foo::Bar(bar) = false;
  let mut Foo { bar } = false;
  let mut Foo { bar, bar } = false;
  let mut Foo { bar: bar } = false;
  let mut Foo { bar: bar, bar: bar } = false;
  let mut Foo { bar: Bar { bar } } = false;

  let mut _ = || 1;
  let mut _ = |a| 1;
  let mut _ = |a, b| 1;
  let mut _ = |a: A| 1;
  let mut _ = |a: A, b| 1;
  let mut _ = |a, b: B| 1;
  let mut _ = |a: A, b: B| 1;

  let mut _ = || { 1 };
  let mut _ = |a| { 1 };
  let mut _ = |a, b| { 1 };
  let mut _ = |a: A| { 1 };
  let mut _ = |a: A, b| { 1 };
  let mut _ = |a, b: B| { 1 };
  let mut _ = |a: A, b: B| { 1 };

  let mut _ = || -> int { 1 };
  let mut _ = |a| -> int { 1 };
  let mut _ = |a, b| -> int { 1 };
  let mut _ = |a: A| -> int { 1 };
  let mut _ = |a: A, b| -> int { 1 };
  let mut _ = |a, b: B| -> int { 1 };
  let mut _ = |a: A, b: B| -> int { 1 };
}
